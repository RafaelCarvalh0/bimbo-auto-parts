version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql-loja
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: lojavirtual
    ports:
      - "3306:3306"